{% extends 'base.html.twig' %}

{% block title %}Hello modif profil!{% endblock %}

{% block body %}
    <div id='main'>
        <div class="container">
            <div class="title">Profil :</div>
                {{ form_start(profilform) }}
                    <div class="user-details">
                        <div class="label">
                            {{ form_row(profilform.name, {
                            label: 'Votre nom : '
                            }) }}
                        </div>

                        <div class="label">
                            {{ form_row(profilform.firstname, {
                            label: 'Votre prénom : '
                            }) }}
                        </div>

                        <div class="label">
                            {{ form_row(profilform.email, {
                            label: 'Votre email : '
                            }) }}
                        </div>

                        <div class="label">
                            <label>Adresse</label>
                            <input type="text" placeholder="Adresse" name="Adresse">
                        </div>

                        <div class="label">
                            <label>Code Postal</label>
                            <input type="text" placeholder="Code Postal" name="Code Postal">
                        </div>

                        <div class="label">
                            <label>Tel</label>
                            <input type="Tel" placeholder="Tel" name="Tel">
                        </div>
                        </div>
                        <div class="button1">
                            {{ form_row(profilform.Valider) }}
                        <div class="button2">

                        </div>
                    </div>
                {{ form_end(profilform) }}
        </div>
    </div>

{% endblock %}
